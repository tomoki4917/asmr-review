---
description: レビュー記事（Markdown）編集後の同期・サーバー再起動・確認URL
globs: src/content/レビュー/**/index.md
alwaysApply: false
---

# レビュー記事を直したあと

1. `npm run sync:content-assets` を実行する（画像・表紙の反映）。
2. 開発サーバーを再起動する（`npm run dev`。既存プロセスがあればポート 3000 を空けてから）。
3. ユーザーへの返答に、**ブラウザで確認する URL** を必ず書く。
   - トップ: `http://localhost:3000/`
   - 該当レビュー: `http://localhost:3000/reviews/<slug>/`（`index.md` の `slug` を使う）

静的書き出しを見る場合は `npm run build` 後に `out/` を開く。
