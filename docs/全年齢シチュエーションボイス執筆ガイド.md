# 全年齢シチュエーションボイス執筆ガイド（運用正本）

**記事グリッド・パート解析・マージ手順・クイック解析の型は、成人向けと同一です。**  
正本は **`docs/シチュエーションボイス執筆ガイド.md`**（`dakimakura-kanojo-pretty-holic-yurukawa-kouhai` 型）。  
本書は **全年齢だけの差分**（五軸第4＝睡眠・覚醒、絶頂行なし、合わない §5.1、tags）を載せます。

| 文書 | 役割 |
|------|------|
| **体裁・パート解析・Gemini キー** | **`docs/シチュエーションボイス執筆ガイド.md`**（矛盾時は本書の全年齢差分が上） |
| **本書** | 全年齢の **五軸表示・採点・合わない・tags** |
| **`docs/全年齢シチュボイス五軸評価定義.md`** | 第4軸 **睡眠・覚醒** の物差し |
| `docs/シチュボイス五軸評価定義.md` | 第1・2・3・5軸（シナリオ／音響／没入度／満足度） |

### 見本記事

| 役割 | 正本 |
|------|------|
| **記事グリッド・パート解析3要素の型** | `dakimakura-kanojo-pretty-holic-yurukawa-kouhai`（**成人向けと同型**） |
| **全年齢のトーン・五軸・合わない** | `shinitagari-junai-maid-yogarekake` |
| **作品感想（`workImpressionParagraphs`）** | `work_impression_shinitagari-junai-maid-yogarekake.json` |

---

## 1. 記事構成（成人向けと同一）

`docs/シチュエーションボイス執筆ガイド.md` §1・§6・§8 に従う。**省略禁止。**

| # | 見出し |
|---|--------|
| 1 | フロントマター（`tags` 先頭 **`全年齢同人`**） |
| 2 | `## 作品名` |
| 3 | `## 作品概要` … `### 基本情報`・`### パートの長さ`（**表形式**・§1.2） |
| 4 | `## 作品評価グラフ` … `**グラフ評価内訳**`（第4軸＝**睡眠・覚醒**） |
| 5 | `**【こんな人におすすめ】**`（3）／`**【合わない可能性がある人】**`（2） |
| 6 | `## 総合評価` … **★／10 のみ**（**絶頂行なし**） |
| 7 | **`## パート解析`** … 引用 → `**物語の流れ:**` → `**感想:**`（§6） |
| 8 | `## 総評：本作品の結論` … 三柱 |
| 9 | **`quickGuideBySlug`** … §9 |

**旧形式（全年齢で使わない）** … `## パート解説`・`**内容:**`・`**体感:**`・`**心情の変化:**`・パート解析省略型。

### 1.1 `### パートの長さ`

成人向けと同じ **`| # | 公式トラック名 | 尺 |` 表**。本編を表にし、おまけ・安眠ループ等は表の下に **合計行付き** で追記可（`shinitagari` 参照）。

### 1.2 総合評価（全年齢差分）

- **`絶頂シーン n回` 行は載せない**（`ORGASM_SCENE_COUNT` も出力しない）。
- それ以外は成人向けと同じ（★のみ）。

---

## 2. 五軸（全年齢差分）

| キー（v2） | 表示名 |
|------------|--------|
| `scores.pleasure` | **睡眠・覚醒**（`radarAxisLabels.pleasure: "睡眠・覚醒"` 推奨） |
| 他4軸 | 成人向けと同じ |

採点順・グラフ内訳の書き方 … **`docs/全年齢シチュボイス五軸評価定義.md`** ＋ シチュガイド §4（括弧サブ点・要因見出し禁止・各軸2〜3文）。

---

## 3. おすすめ／合わない

シチュガイド §5 ＋ **`§5.1 全年齢専用`**（R18 欠如を合わない理由にしない）。  
クイック … **ラベルのみ**（§5.2）。

---

## 4. パート解析

**`docs/シチュエーションボイス執筆ガイド.md` §6 をそのまま適用。**

- `#### 場面見出し` → `>` 引用 → `**物語の流れ:**`（2文）→ `**感想:**`（1〜2文）
- おまけ・ループ単体は**載せない**（採点外）
- **`**体感:**` 禁止**（監査 `all_ages_no_taikan`）

---

## 5. 作品感想（`workImpressionParagraphs`）

シチュガイド §8.4 同型。文体見本 … `work_impression_shinitagari-junai-maid-yogarekake.json`（文言コピー禁止）。

---

## 6. Gemini パイプライン

```bash
cd scripts/gemini-hypnosis-review
py -3 auto_review.py --scenario-voice --all-ages --writer-only --skip-eval --force \
  --slug <slug> --item-name "…" --circle "…" --rj RJ… \
  --sale-date YYYY-MM-DD --published-at YYYY-MM-DD \
  --analysis-dir "…"

py -3 merge_scenario_index.py <slug> --all-ages \
  --item-name "…" --circle "…" --rj RJ… \
  --sale-date YYYY-MM-DD --published-at YYYY-MM-DD \
  [--go-live-at "…"]
```

- **`--all-ages`** … 執筆ガイドに本書を載せる・グラフ alt に **睡眠・覚醒**・マージ時 **絶頂行省略**・`radarAxisLabels` 同期。
- 必須 `[KEY]` … 成人向けと同じ（**`PART_ANALYSIS` 必須**）。

---

## 7. 執筆後チェック

シチュガイド §11 ＋

- [ ] `tags` 先頭 `全年齢同人`
- [ ] **`## パート解析`** あり（引用→物語の流れ→感想）
- [ ] 総合評価に **絶頂行なし**
- [ ] グラフ第4軸 **睡眠・覚醒**
- [ ] 合わない §5.1 遵守
- [ ] `npm run review:validate-prose`・`review:audit-scenario`・`review:audit-kansei`

---

## 8. 関連

| ファイル | 用途 |
|----------|------|
| `.cursor/rules/review-all-ages-doujin-canon.mdc` | Cursor 判定 |
| `merge_scenario_index.py --all-ages` | index 組み立て |
| `writer_output_keys_scenario.md` | `[KEY]`（全年齢も同一） |

**改版:** 2026-06-02 成人向けシチュガイドと**同一グリッド**へ統一（パート解析必須・表形式・絶頂行なしのみ差分）
