{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://asmr-review.local/schemas/review-analysis.v1.schema.json",
  "title": "催眠音声レビュー — 分析データ（v1）",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "workName", "scores"],
  "properties": {
    "schemaVersion": {
      "type": "integer",
      "const": 1,
      "description": "スキーマ版。将来の互換用。"
    },
    "workName": {
      "type": "string",
      "minLength": 1,
      "description": "作品名（旧テキストの「作品名:」に相当）"
    },
    "scores": {
      "type": "object",
      "additionalProperties": false,
      "required": ["trance", "pleasure", "satisfaction"],
      "properties": {
        "trance": {
          "type": "number",
          "minimum": 0,
          "maximum": 10,
          "description": "トランス度（レーダー・本文と一致）。標準採点では先に決め、快楽・満足と整合させる（docs/レビュー三軸評価定義.md「トランスありき」）"
        },
        "pleasure": {
          "type": "number",
          "minimum": 0,
          "maximum": 10,
          "description": "快楽度（催眠として提案が届いた前提の報酬設計として読む）"
        },
        "satisfaction": {
          "type": "number",
          "minimum": 0,
          "maximum": 10,
          "description": "満足度（催眠体験の着地・約束の回収。第三軸ラベルは thirdAxisLabel）"
        }
      }
    },
    "thirdAxisLabel": {
      "type": "string",
      "description": "第三軸の表示ラベル。省略時は「満足度」。"
    },
    "orgasmSummary": {
      "type": "string",
      "description": "絶頂目安（本文の総合評価と同期させる一行。任意）"
    },
    "notes": {
      "type": "array",
      "items": { "type": "string" },
      "description": "評価方針・参照データ・軸ごとのメモなど（旧 # コメント行）"
    }
  }
}
